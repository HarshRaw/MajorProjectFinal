@model IEnumerable<MajorProject.Models.CarModel>

@{
    ViewData["Title"] = "Index";
}
<div style="padding-top:6rem;padding-left:2rem;padding-right:2rem">

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CarCompanies)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CarModels)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CarCompanies.CarCompanies)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CarModels)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CarCompanyId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CarCompanyId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CarCompanyId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</div>