@model IEnumerable<MajorProject.Models.Customer>

@{
    ViewData["Title"] = "Index";
}
<div style="padding-top:6rem;padding-left:2rem;padding-right:2rem">

<h1>Index</h1>

<p>
@*    <a asp-action="Create">Create New</a>
*@        <a asp-area="OnWayVehicleService" asp-controller="Cars"  asp-action="Create">Add Your Car</a>

</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CustomerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MobileNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MobileNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
@*                <a asp-action="Edit" asp-route-id="@item.CustomerId">Add Another Car</a> |
*@                <a asp-action="Details" asp-route-id="@item.CustomerId">Details</a> 
                @*<a asp-action="Delete" asp-route-id="@item.CustomerId">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
</div>