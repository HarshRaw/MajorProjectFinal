@model IEnumerable<MajorProject.Models.IssueCategory>

@{
    ViewData["Title"] = "Index";
}
<div style="padding-top:6rem;padding-left:2rem;padding-right:2rem">

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Issue)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Issue)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IssueCategoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IssueCategoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IssueCategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</div>
